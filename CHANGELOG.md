# Change Log

## Unreleased

### 変更

- 既定の Raw データ層が `roots: []` を持たなくなり，通常のワークスペース内レイヤーになった．`patterns` は `contents/**` `raw/**` `data/**` `attachments/**`．**既定設定の利用者はこれらの配下のファイルが「その他」から「Raw データ層」へ移動する**．first-match-wins のため `contents/*.md` と `contents/*.csv` は従来どおりナレッジベース層・オントロジー層に入る．`roots` を明示設定している利用者への影響は無い
- `roots` を宣言しているのに走査先が 1 つも無いレイヤーについて，パネルが黙って空になる代わりに警告を 1 度だけ表示するようにした

### 追加

- `layeredkb.followSymlinks` 設定（既定 `false`）．レイヤーの `roots` 配下を走査するときシンボリックリンクを辿る．Google Drive のマウントをリンクで置く構成向け．循環参照を検出して打ち切り，深さ 64・エントリ 20 万の上限を設け，打ち切り時はユーザーに通知する
- 設定が無効なときも，スキップしたシンボリックリンクの件数を警告として報告するようにした


## 0.1.0

- アクティビティバーに LayeredKB コンテナを追加．スキーマ層 / オントロジー層 / ナレッジベース層 / Raw データ層 / その他 をそれぞれ独立したパネルとして表示
- `layeredkb.layers` 設定でレイヤーを自由に定義可能（first-match-wins，最大 8 個）
- レイヤーの `roots` で Google Drive など Git 管理外のフォルダを走査・表示
- 標準エクスプローラーにレイヤーのバッジと色を付ける FileDecorationProvider
- コンパクトフォルダー表示，ファイル監視による自動更新，マルチルート対応
- ファイルのコンテキストメニュー（横に開く・エクスプローラーで表示・OS で表示・パスのコピー）
